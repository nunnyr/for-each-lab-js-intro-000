function iterativeLog (index, element) {
  console.log(`${index}: ${element}`)
}


function iterate(callback) {
  let dogs ['nico']
  nico.forEach(callback)
  return dogs 

}

function doToArray(array, callback) {
  array.forEach(callback)
}